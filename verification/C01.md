# StructDet Code C01 delivery

Completed 2026-09-23. Development version: **0.1.0.dev1**.

## Delivered

- One specification fixing task resolution, evidence policies, the compact input
  contract, revision identity and the six-increment delivery boundary.
- An independent Python package with working `validate` and `inspect` commands,
  exact support/SCI/Gini-Simpson calculations and JSON/Markdown output.
- A source-bound sorting fixture with seven source files, seven stipulated run
  roots, ten revision records, actual finite test receipts and explanatory
  mechanism anchors.
- Explicit states for accepted/proposed/unresolved/conflicted labels, missing
  history, partial capture, validity and unknown evidence.
- Source, assignment, suite, revision and feedback binding checks; passive,
  bounded, no-follow file intake; one selected observation per run.

## Verification actually performed

Environment: Linux, CPython 3.12.14. No third-party runtime dependencies.

| Check | Result |
| --- | --- |
| Canonical focused tests | **23 passed** |
| Pinned Bench arithmetic comparison | **13 count tables matched**, including empty/zero, single-class, balanced, skewed and large integer-count cases |
| `validate` on the shipped fixture | Exit 0, valid_records, substantive_validation_performed=false |
| `inspect` JSON and Markdown | Exit 0; both expose the same canonical metrics |
| Project-owned function executions | **100** retained finite test executions, ten cases for each of ten revision receipts |
| External model calls | **0** |

The current tests exercise incorrect denominators, provisional-label promotion,
source changes, stale reclassification, receipt misbinding across revisions,
changed suites, incomplete passes, no-op inflation, future-parent links,
feedback visibility, explicitly missing history, unsafe paths, symlinks,
executable hook fields, a candidate side-effect canary, malformed JSON and
JSON/Markdown agreement. These checks target the C01 boundary; no general
security certification or mutation score is asserted.

Raw results: `tests.txt`, `validate.json`, `minimal_report.json`,
`minimal_report.md`, `bench_parity.json` and `c01_verification.json`.

## Observable fixture result

Seven explicitly selected initial revisions produce:

| View | Admitted n | Counts | Observed support | Exact SCI |
| --- | ---: | --- | ---: | ---: |
| All admitted mechanisms | 5 | INS 3, MERGE 1, SEL 1 | 3 | 11/25 |
| Admitted mechanisms with passing finite validity | 4 | INS 2, MERGE 1, SEL 1 | 3 | 3/8 |

The remaining selected records consist of one unresolved opaque implementation
and one unadmitted model-style proposal. Five of the seven selected records have
passing finite tests plus supplied passing conformance; only four of those also
have admitted classifications. This preserves separate correctness and
classification denominators.

Changing the explicit selection from R1V0 to its input-copy repair R1V1 raises
the valid-classified population from four to five while preserving the all-label
mechanism counts. Selecting the unchanged-byte R1V2 again preserves the counts.
Separate run roots can share exact source bytes without being deduplicated into
one attempt. A receipt from R1V2 cannot certify R1V1 even when the bytes match.

These are designed software-fixture observations. The revision and feedback
history is stipulated, source mechanism assignments are fixture judgments, and
the finite test executions are actual local runs of reviewed project-owned code.
No live coding-agent experiment, classifier generalization or causal effect was
established.

## Reuse and licensing

Bench baseline: `0a9dc88deffd4b14264485b161bea06f027f68f5`, version 0.1.0.
Code implements the explicitly stated count mathematics independently and checks
it against the pinned upstream count component. It does not depend on or bypass
the Bench task/evidence pipeline. The parity process is not qualification of
Bench's Python 3.13 package on Python 3.12.

Software uses Apache-2.0. Documentation, descriptors, fixture data and reports
use CC BY 4.0. The adapted sorting descriptors retain their original CC BY 4.0
attribution. The three theory PDFs retain their own publication licenses and
are excluded. Exact supplied-source fingerprints are in c01_verification.json.

## Current limits and next increment

C01 is a working development foundation. Automatic mechanism recognition,
review-template preparation and convenient intake are C02. The graph task and
condition comparisons are C03. Full trajectory/cohort convergence is C04.
Integrated intervention and strict replay are C05. Distribution installation and
supported-platform qualification are C06.

At the original C01 handoff, no wheel, package upload, new remote repository, PR,
merge, tag or public release had been created. No additional owner decision
blocks C02.

## Reproduce from source

From the extracted `structdet-code` directory:

```bash
python3 -m structdet_code validate --study examples/minimal/study.json
python3 -m structdet_code inspect --study examples/minimal/study.json --format markdown
python3 -m unittest discover -s tests -v
```

For optional upstream arithmetic parity, supply an explicitly trusted checkout
of the pinned Bench commit. The command verifies the required module digests:

```bash
python3 tools/check_bench_parity.py --bench-root /absolute/path/to/trusted/structdet-bench
```

The public inspection path does not run candidates. The separate developer
fixture builder executes only the seven named project fixture files and should
be used only on those reviewed files.
